from constants import PG_PASSWORD, PG_USER, PG_HOST, PG_PORT, PG_DATABASE, PORT

config = {
    PG_USER: "postgres",
    PG_PASSWORD: "admin",
    PG_HOST: "localhost",
    PG_PORT: 5432,
    PG_DATABASE: "postgres",
    PORT: 5001
}